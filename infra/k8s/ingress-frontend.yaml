apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: velocity-frontend
  namespace: frontend
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: web
spec:
  ingressClassName: traefik
  rules:
    - host: velocity.builderwire.net
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: velocity-frontend
                port:
                  number: 80
